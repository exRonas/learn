    # Автоматическое меню домашних заданий

Меню наполняется из файла `homeworks.json`, который генерируется скриптом `build-homeworks.js`.

## Как добавить новое задание
1. Создай папку: `homework4` (номер следующий).
2. Внутри сделай файл `index.html`.
3. Запусти скрипт генерации:
   ```powershell
   node build-homeworks.js
   ```
4. Открой любую страницу учебника — новые ссылки появятся автоматически.

## Если надо кастомное название
Открой `homeworks.json` и замени поле `title` у нужного объекта. Например:
```json
{
  "folder": "homework4",
  "title": "Домашнее задание 4 (Flexbox)"
}
```
После ручного изменения перезапуск скрипта его НЕ затрёт, если ты оставишь формат.

## Как работает
- Скрипт ищет директории в корне, совпадающие с регуляркой `homework\d+`.
- Формирует массив объектов `{ folder, title }`.
- Записывает `homeworks.json`.
- `app.js` при загрузке страницы делает `fetch('homeworks.json')` и строит ссылки внутри `<ul id="homeworkMenu">` + список на странице `homeworks.html`.

## Частые вопросы
- Почему не читаем папки прямо из браузера? В браузере нельзя получить список директорий файловой системы ради безопасности.
- Можно ли держать всё без Node? Тогда нужно вручную править `homeworks.json`.
- Можно ли хранить описания? Добавь поле `description` в объект — оно сейчас не используется, но можешь расширить код.

Успехов! 🚀
