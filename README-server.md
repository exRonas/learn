# Локальный авто-поиск папок homeworkN

Теперь меню домашних заданий формируется автоматически при открытии страниц через Node сервер.

## Запуск
```powershell
node server.js
```
Открой в браузере: http://localhost:3000/

## Добавление нового задания
1. Создай папку `homework4` (следующий номер).  
2. Внутри добавь файл `index.html`.  
3. Перезагрузи страницу в браузере — ссылка появится сама.

Никаких дополнительных команд не нужно: сервер сам сканирует корень на каждый запрос `/api/homeworks`.

## Как это работает
- `server.js` обслуживает статические файлы и эндпоинт `/api/homeworks`.  
- Эндпоинт ищет директории, подходящие под шаблон `homework\d+`.  
- `app.js` делает `fetch('/api/homeworks')` и вставляет ссылки в `<ul id="homeworkMenu">` и список на `homeworks.html` (если присутствует).

## Возможные улучшения
- Кэширование списка (сейчас не нужно — мало директорий).  
- Добавление описаний (можно расширить объект: `{ folder, title, description }`).  
- Сортировка другая (сейчас растёт по номеру).  

## Частые вопросы
**Почему нельзя просто в браузере прочитать папки?**  
Из соображений безопасности браузер не имеет доступа к произвольной файловой системе. Нужен сервер.

**Нужно ли ставить зависимости?**  
Нет, используется чистый `http` модуль Node.

Удачи! 🚀

## Запуск через PM2 (продвинутый режим)
PM2 позволяет держать `server.js` запущенным в фоне, автоматически перезапускать при падении и удобно управлять процессом.

### 1. Установка PM2 (глобально)
```powershell
npm install -g pm2
```

Проверь установку:
```powershell
pm2 -v
```

### 2. Старт сервера
```powershell
pm2 start server.js --name learn-server
```

Если нужен автоматический перезапуск при изменении файла (простая разработка):
```powershell
pm2 start server.js --name learn-server --watch
```
`--watch` мониторит изменения в каталоге (не для продакшена: чуть выше расход CPU).

### 3. Список и статус процессов
```powershell
pm2 list
pm2 status learn-server
```

### 4. Логи
```powershell
pm2 logs learn-server
pm2 logs --lines 100
```

### 5. Перезапуск / остановка / удаление
```powershell
pm2 restart learn-server
pm2 stop learn-server
pm2 delete learn-server
```

### 6. Автозапуск после перезагрузки системы (Windows)
PM2 умеет генерировать конфигурацию запуска:
```powershell
pm2 save          # Сохраняет текущий список процессов
pm2 startup       # Покажет команду для регистрации (выполни её)
```
Если команда `pm2 startup` выведет дополнительную строку — скопируй и выполни её (обычно с правами администратора).

### 7. Обновление после изменения кода
После правок в `server.js` (если не используешь `--watch`):
```powershell
pm2 restart learn-server
```

### 8. Экспорт / импорт процесса
Команда `pm2 save` сохраняет конфиг, чтобы он восстановился после рестарта. Если нужно очистить всё:
```powershell
pm2 delete all
pm2 save
```

### 9. Быстрая проверка порта
Открой: http://localhost:3000/ — если отвечает, значит процесс активен.

### 10. Полезные команды кратко
```powershell
pm2 list
pm2 start server.js --name learn-server
pm2 restart learn-server
pm2 logs learn-server
pm2 stop learn-server
pm2 delete learn-server
pm2 save
pm2 startup
```

Теперь можно не держать отдельное окно PowerShell открытым: PM2 управляет процессом в фоне.
